apply from: "../../common_module.gradle"

android {
    resourcePrefix "widget_" //给 Module 内的资源名增加前缀, 避免资源名冲突
}

dependencies {
    implementation project(':component:common_widget')
    implementation project(':component:common_sdk')
}
